services:
  - type: web
    name: quizspark
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist-server/server/index.js
    envVars:
      - key: NODE_ENV
        value: production
